[build]
builder = "nixpacks"

[deploy]
startCommand = "poetry run python -m src.agent_a2a"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
